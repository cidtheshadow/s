{
  "version": 2,
  "installCommand": "npm install --include-workspace-root --workspace=@kisanify/backend --workspace=@kisanify/shared",
  "buildCommand": "npm run build",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
