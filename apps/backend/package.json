{
  "name": "@kisanify/backend",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "npm --prefix ../../packages/shared run build && npm --prefix ../mobile run build:web && mkdir -p ../../public && cp -R ../mobile/dist/* ../../public/",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kisanify/shared": "file:../../packages/shared",
    "@supabase/supabase-js": "^2.39.8",
    "hono": "^4.1.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240320.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.35.0"
  }
}
